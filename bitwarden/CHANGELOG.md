## What’s new in 1.36.0

## What's Changed

- SSO fallback to UserInfo preferred_username
- Dummy identifier need to pass for a guid
- add new /identity/accounts/prelogin/password
- Add DuckDuckGo browser device type
- Apply duration_suboptimal_units lint findings
- Apply ref_option lint findings
- Fix hardcoded sso identifier
- Update crates and fix a nightly lint
- Fix Host/IP resolving
- Several SSO Fixes
- Add support for archiving items
- Fix favicon fetching to check all icon links instead of just the first one
- Fix merge conflict
- Replace organization_uuid unwrap with proper error handling
- fix: return Err instead of panic on unknown cipher atype in to_json()
- Allow SQLite to be linked against dynamically
- Update crates and web-vault
- Update hickory